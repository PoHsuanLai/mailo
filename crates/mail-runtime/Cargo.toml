[package]
name = "mail-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
mail-domain.workspace = true
mail-proto.workspace = true
mail-store.workspace = true
mail-mime.workspace = true
mail-pim.workspace = true
chrono.workspace = true
thiserror.workspace = true
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util", "time", "sync"] }
tokio-rustls = "0.26"
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
webpki-roots = "1"
rustls-pki-types = "1"
blake3 = "1"
rusqlite = { version = "0.40", features = ["bundled"] }
# default-features off: oauth2 would otherwise pull its own reqwest and make the HTTP call
# itself. We want the call here, where the runtime already owns TLS and timeouts.
oauth2 = { version = "5", default-features = false }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json", "charset", "http2"] }
url = "2"
base64 = "0.22"
serde = { workspace = true }
serde_json = "1"
uuid = { workspace = true }
keyring = "4"
# Account discovery's SRV and MX lookups (RFC 6186). MIT OR Apache-2.0; the system resolver's
# configuration, over tokio. Its DNS-over-TLS/HTTPS features stay off.
hickory-resolver = "0.26"
# The registered domain of an MX host, for account discovery. Already here through mail-app.
psl = "2"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
# The fake CardDAV server in `tests/carddav.rs` reads the hrefs a multiget asks for.
roxmltree = "0.21"
tempfile = "3"
# Seeded key generation for the OpenPGP tests, and the WKD hash they check a request path by.
# Both already in the tree through rPGP.
rand = "0.8"
sha1 = "0.10"

[lints]
workspace = true
